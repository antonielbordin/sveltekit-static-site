{
  "name": "sveltekit-static-site",
	"version": "0.0.1",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "svelte-kit dev",
    "build": "svelte-kit build",
    "deploy-gh-pages": "touch build/.nojekyll && cp -r build docs"
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "next",
    "@sveltejs/kit": "next",
    "gh-pages": "^4.0.0",
    "svelte": "^3.48.0"
  }
}
